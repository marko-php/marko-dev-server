# marko/dev-server

Start your full development environment with a single command.

## Installation

```bash
composer require marko/dev-server
```

## Quick Example

```bash
marko dev:up        # Start PHP server, Docker, and frontend tools (detached by default)
marko dev:up -f     # Start in foreground mode (press Ctrl+C to stop)
marko dev:status    # Check what's running
marko dev:down      # Stop everything
```

## Documentation

Full usage, configuration, and API reference: [marko/dev-server](https://marko.build/docs/packages/dev-server/)
